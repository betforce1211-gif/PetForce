# ============================================
# PLAYWRIGHT TEST ENVIRONMENT CONFIGURATION
# ============================================
#
# This file ensures E2E tests use mocked API responses
# instead of hitting the real Supabase instance.
#
# The mock URL (https://test.supabase.co) matches the
# URL pattern in test-helpers.ts, allowing Playwright
# to intercept and mock all API calls.
#
# DO NOT modify these values unless you also update
# the mock setup in test-helpers.ts.
# ============================================

# Mock Supabase URL - matches what test-helpers.ts expects
VITE_SUPABASE_URL=https://test.supabase.co

# Mock anon key - not a real key, just for test configuration
VITE_SUPABASE_ANON_KEY=mock-anon-key-for-testing-only

# Ensure we're in test mode
NODE_ENV=test
